<Window x:Class="CodexStatus.Tray.SessionsFlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Codex Sessions"
        Width="540"
        SizeToContent="Height"
        MaxHeight="640"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        Deactivated="OnDeactivated"
        PreviewKeyDown="OnPreviewKeyDown">
    <Border Background="#F8FAFC" BorderBrush="#CBD5E1" BorderThickness="1" Padding="12">
        <DockPanel LastChildFill="True">
            <TextBlock DockPanel.Dock="Top"
                       Text="Codex Sessions"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="#0F172A"
                       Margin="0,0,0,8" />
            <ListBox x:Name="SessionsList"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="0,8">
                            <StackPanel>
                                <TextBlock Text="{Binding Label}" FontWeight="SemiBold" Foreground="#0F172A" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding Meta}" Foreground="#475569" FontSize="12" Margin="0,2,0,0" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding Detail}" Foreground="#64748B" FontSize="12" Margin="0,2,0,0" TextWrapping="Wrap" />
                                <TextBlock Text="{Binding Payload}"
                                           Foreground="#334155"
                                           FontFamily="Consolas"
                                           FontSize="11"
                                           Margin="0,6,0,0"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Border>
</Window>
